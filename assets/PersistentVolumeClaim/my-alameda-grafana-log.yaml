apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: my-alameda.grafana-log.pvc
  namespace: {{.NameSpace}}
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 2Gi